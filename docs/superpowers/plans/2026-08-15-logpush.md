# Logpush Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Enviar os logs de `/var/log/argus/**` para S3 e webhooks de chat, configurável pela interface web, sem perder nem duplicar mensagem.

**Architecture:** Um coletor (`logpush.py`) lê o que há de novo em cada arquivo a partir de um ponteiro `(inode, posição)`, e entrega a destinos plugáveis que implementam `LogDestination.send()`. O coletor não conhece nenhum destino; trocar de destino é trocar uma linha no config. Execução por `systemd timer` a cada 5 minutos.

**Tech Stack:** Python 3.11+ (stdlib), `boto3` (S3), `requests` (webhook — já usado no projeto), `unittest` (stdlib) para testes.

## Global Constraints

- **Padrão plugável obrigatório:** base `LogDestination` + subclasse por destino + registry, conforme `.docs/llm-integration-standard.md`. Subclasse implementa **apenas** o transporte.
- **Objetos S3 são write-once.** Nunca reescrever um objeto já enviado — o `pull-logs-s3` usa watermark por `LastModified` (`pull_s3_logs.py:188`) e reescrita causa duplicação em massa.
- **Ponteiro avança somente após confirmação do destino.** Falha transitória não pode virar perda.
- **Falha silenciosa é proibida.** Todo erro registra motivo (status HTTP / exceção).
- **Segredos nunca voltam inteiros pela API** — só `mask()`. Config em `640 root:argus`.
- **Sem dependência nova além de `boto3`.** Testes em `unittest` (stdlib).
- **Arquivos gravados com `newline=''`** (LF) — CRLF quebra o hook do ShellCheck.
- **Nomenclatura em português** nos identificadores de domínio, como no resto do projeto.
- Rodar `ruff check .` e `python3 -m unittest discover -s tests` antes de cada commit.

## File Structure

| Arquivo | Responsabilidade |
|---|---|
| `core/logpush_config.py` | Catálogo de destinos/origens, leitura e escrita do config, mascaramento, validação de URL. |
| `core/logpush_dest/base.py` | Contrato `LogDestination` + `LogPushError` + registry. |
| `core/logpush_dest/s3.py` | Transporte S3 (boto3), nomeação anticolisão. |
| `core/logpush_dest/webhook.py` | Transporte HTTP, payload por plataforma, filtro de severidade. |
| `core/logpush_fmt.py` | Parser RFC 5424 → evento; evento → texto legível. |
| `core/logpush.py` | Coletor: ponteiro, rotação, lote, orquestração. |
| `core/webapp.py` (modificar) | API `/api/logpush` (GET/POST) e `/api/logpush/test`. |
| `core/reporter.py` (modificar) | Página **Logpush**. |
| `install.sh` (modificar) | Timer systemd, permissões, cópia dos módulos, `python3-boto3`. |
| `tests/test_logpush_*.py` | Testes por módulo. |

---

### Task 1: Config do logpush

**Files:**
- Create: `core/logpush_config.py`
- Test: `tests/test_logpush_config.py`

**Interfaces:**
- Consumes: nada (primeira task).
- Produces: `ORIGENS: list[dict]`, `DESTINOS: list[dict]`, `config_path() -> Path`, `ler() -> dict`, `gravar(cfg: dict) -> None`, `mascarar(valor: str) -> str`, `url_segura(url: str) -> bool`, `origens_ligadas(cfg: dict) -> list[str]`, `PLACEHOLDER: str`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_config.py`:

```python
import os
import tempfile
import unittest
from pathlib import Path

import sys
sys.path.insert(0, "core")
import logpush_config as LC


class TestMascarar(unittest.TestCase):
    def test_nunca_devolve_valor_inteiro(self):
        self.assertEqual(LC.mascarar("AKIAIOSFODNN7EXAMPLE"), "••••MPLE")

    def test_vazio_vira_vazio(self):
        self.assertEqual(LC.mascarar(""), "")
        self.assertEqual(LC.mascarar(LC.PLACEHOLDER), "")

    def test_valor_curto_nao_vaza(self):
        self.assertEqual(LC.mascarar("abc"), "••••")


class TestUrlSegura(unittest.TestCase):
    def test_https_publico_ok(self):
        self.assertTrue(LC.url_segura("https://chat.googleapis.com/v1/spaces/AAA"))

    def test_http_recusado(self):
        self.assertFalse(LC.url_segura("http://chat.googleapis.com/x"))

    def test_ip_privado_recusado(self):
        for u in ("https://127.0.0.1/x", "https://10.0.0.5/x",
                  "https://192.168.1.10/x", "https://172.16.0.1/x",
                  "https://localhost/x", "https://169.254.169.254/x"):
            self.assertFalse(LC.url_segura(u), u)

    def test_lixo_recusado(self):
        self.assertFalse(LC.url_segura(""))
        self.assertFalse(LC.url_segura("file:///etc/passwd"))


class TestOrigens(unittest.TestCase):
    def test_origens_ligadas_le_do_config(self):
        cfg = {"origem_monitor": True, "origem_audit": True, "origem_scan": False}
        self.assertEqual(sorted(LC.origens_ligadas(cfg)), ["audit", "monitor"])

    def test_catalogo_tem_as_sete_origens(self):
        ids = {o["id"] for o in LC.ORIGENS}
        self.assertEqual(ids, {"monitor", "submonitor", "credentials",
                               "email", "typosquat", "audit", "scan"})


class TestGravarLer(unittest.TestCase):
    def test_roundtrip(self):
        with tempfile.TemporaryDirectory() as d:
            os.environ["ARGUS_BASE"] = d
            LC.gravar({"destino": "s3", "s3_bucket": "meu-bucket"})
            self.assertEqual(LC.ler()["s3_bucket"], "meu-bucket")

    def test_ler_sem_arquivo_devolve_vazio(self):
        with tempfile.TemporaryDirectory() as d:
            os.environ["ARGUS_BASE"] = d
            self.assertEqual(LC.ler(), {})


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_config -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush_config'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush_config.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush_config — catálogo e configuração do envio de logs para fora.

Espelha a postura de `providers.py`: o segredo mora no arquivo de config (fora do
repositório), a interface só recebe um resumo mascarado, e a gravação é in-place
para o serviço precisar de permissão apenas NO ARQUIVO, nunca no diretório.
"""

from __future__ import annotations

import ipaddress
import json
import os
import socket
from pathlib import Path
from urllib.parse import urlparse

PLACEHOLDER = "SUA_CHAVE_AQUI"

# Origens de log que podem ser enviadas. `caminho` é relativo a /var/log/argus.
# `estruturado` diz se o conteúdo é RFC 5424 (uma linha = uma mensagem) ou texto
# cru de execução (o arquivo inteiro é uma mensagem).
ORIGENS: list[dict] = [
    {"id": "monitor",     "label": "Portas",             "caminho": "monitor/monitor.log",   "estruturado": True},
    {"id": "submonitor",  "label": "Subdomínios",        "caminho": "submonitor/*.log",      "estruturado": True},
    {"id": "credentials", "label": "Credenciais",        "caminho": "credentials/*.log",     "estruturado": True},
    {"id": "email",       "label": "Postura de e-mail",  "caminho": "email/*.log",           "estruturado": True},
    {"id": "typosquat",   "label": "Typosquat",          "caminho": "typosquat/*.log",       "estruturado": True},
    {"id": "audit",       "label": "Auditoria",          "caminho": "audit/audit.log",       "estruturado": True},
    {"id": "scan",        "label": "Saída de execução",  "caminho": "scan/*.log",            "estruturado": False},
]
POR_ID = {o["id"]: o for o in ORIGENS}

# Destinos e seus campos. `segredo` marca o que nunca volta inteiro pela API.
DESTINOS: list[dict] = [
    {"id": "s3", "label": "Bucket S3 (ou compatível)",
     "campos": [
         {"nome": "s3_bucket",     "label": "Bucket",            "segredo": False},
         {"nome": "s3_prefixo",    "label": "Prefixo",           "segredo": False, "padrao": "logs/argus"},
         {"nome": "s3_regiao",     "label": "Região",            "segredo": False, "padrao": "us-east-1"},
         {"nome": "s3_endpoint",   "label": "Endpoint (MinIO/R2, opcional)", "segredo": False},
         {"nome": "s3_access_key", "label": "Access key",        "segredo": True},
         {"nome": "s3_secret_key", "label": "Secret key",        "segredo": True},
     ]},
    {"id": "webhook", "label": "Webhook (chat)",
     "campos": [
         {"nome": "webhook_url",       "label": "URL do webhook", "segredo": True},
         {"nome": "webhook_plataforma", "label": "Plataforma",    "segredo": False, "padrao": "google_chat"},
         {"nome": "webhook_chat_id",   "label": "Chat ID (Telegram)", "segredo": False},
     ]},
]
DEST_POR_ID = {d["id"]: d for d in DESTINOS}

PLATAFORMAS = ["google_chat", "slack", "discord", "teams", "telegram", "generico"]
SEVERIDADES = ["CRITICO", "ALTO", "MEDIO", "BAIXO", "INFO"]
SEV_PADRAO = ["CRITICO", "ALTO"]


def base_dir() -> Path:
    return Path(os.environ.get("ARGUS_BASE", "/etc/argus"))


def config_path() -> Path:
    return Path(os.environ.get("ARGUS_LOGPUSH_CONFIG", str(base_dir() / "logpush.json")))


def ler() -> dict:
    try:
        dados = json.loads(config_path().read_text(encoding="utf-8"))
        return dados if isinstance(dados, dict) else {}
    except Exception:
        return {}


def gravar(cfg: dict) -> None:
    """Grava IN-PLACE: o serviço precisa de permissão só no arquivo, nunca no
    diretório. Restaura o conteúdo anterior se a escrita falhar no meio."""
    caminho = config_path()
    corpo = json.dumps(cfg, ensure_ascii=False, indent=4) + "\n"
    try:
        anterior = caminho.read_text(encoding="utf-8") if caminho.exists() else ""
    except OSError:
        anterior = ""
    try:
        with open(caminho, "w", encoding="utf-8", newline="") as fh:
            fh.write(corpo)
            fh.flush()
            os.fsync(fh.fileno())
    except Exception:
        if anterior:
            try:
                with open(caminho, "w", encoding="utf-8", newline="") as fh:
                    fh.write(anterior)
                    fh.flush()
                    os.fsync(fh.fileno())
            except OSError:
                pass
        raise


def mascarar(valor: str) -> str:
    """Resumo seguro — nunca devolve o valor real."""
    v = str(valor or "").strip()
    if not v or v == PLACEHOLDER:
        return ""
    return "••••" + v[-4:] if len(v) > 4 else "••••"


def url_segura(url: str) -> bool:
    """Só HTTPS para host público.

    Recusa IP privado, loopback, link-local e nomes que resolvem para eles: sem
    isso o Argus viraria um pivô para varrer a rede interna de quem o hospeda
    (SSRF) — justamente o que a ferramenta existe para denunciar.
    """
    try:
        p = urlparse(str(url or "").strip())
    except Exception:
        return False
    if p.scheme != "https" or not p.hostname:
        return False
    host = p.hostname
    try:
        infos = socket.getaddrinfo(host, None)
    except Exception:
        return False
    for info in infos:
        try:
            ip = ipaddress.ip_address(info[4][0])
        except ValueError:
            return False
        if ip.is_private or ip.is_loopback or ip.is_link_local or ip.is_reserved:
            return False
    return True


def origens_ligadas(cfg: dict) -> list[str]:
    return [o["id"] for o in ORIGENS if bool(cfg.get(f"origem_{o['id']}", False))]


def severidades_ligadas(cfg: dict) -> list[str]:
    marcadas = [s for s in SEVERIDADES if bool(cfg.get(f"sev_{s}", False))]
    return marcadas or SEV_PADRAO
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_config -v`
Expected: PASS (11 testes)

Nota: `test_ip_privado_recusado` com `https://localhost/x` depende de resolução DNS local; se o ambiente de CI não resolver `localhost`, `getaddrinfo` levanta e a função devolve `False` — o teste passa igual.

- [ ] **Step 5: Commit**

```bash
git add core/logpush_config.py tests/test_logpush_config.py
git commit -m "feat(logpush): catalogo, config e validacao de URL"
```

---

### Task 2: Contrato dos destinos

**Files:**
- Create: `core/logpush_dest/__init__.py`, `core/logpush_dest/base.py`
- Test: `tests/test_logpush_base.py`

**Interfaces:**
- Consumes: nada.
- Produces: `LogPushError(Exception)`, `class LogDestination` com `__init__(self, cfg: dict)` e `send(self, mensagens: list[Mensagem]) -> None`, `Mensagem` (dataclass: `origem: str`, `texto: str`, `quando: datetime`, `severidade: str`, `msgid: str`, `campos: dict`), `registrar(nome)` (decorator) e `criar(cfg: dict) -> LogDestination`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_base.py`:

```python
import datetime
import unittest

import sys
sys.path.insert(0, "core")
from logpush_dest import base as B


class TestRegistry(unittest.TestCase):
    def test_criar_devolve_destino_registrado(self):
        @B.registrar("fake")
        class Fake(B.LogDestination):
            def send(self, mensagens):
                self.recebidas = mensagens

        d = B.criar({"destino": "fake"})
        self.assertIsInstance(d, Fake)

    def test_destino_desconhecido_levanta(self):
        with self.assertRaises(B.LogPushError):
            B.criar({"destino": "nao_existe"})

    def test_base_nao_pode_ser_usada_direto(self):
        with self.assertRaises(NotImplementedError):
            B.LogDestination({}).send([])


class TestMensagem(unittest.TestCase):
    def test_campos_obrigatorios(self):
        m = B.Mensagem(origem="monitor", texto="linha crua",
                       quando=datetime.datetime(2026, 8, 15, 13, 35, 47),
                       severidade="CRITICO", msgid="PORT_NEW", campos={"ip": "1.2.3.4"})
        self.assertEqual(m.origem, "monitor")
        self.assertEqual(m.campos["ip"], "1.2.3.4")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_base -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush_dest'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush_dest/__init__.py`:

```python
"""Destinos de logpush — um módulo por transporte."""
```

Create `core/logpush_dest/base.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush_dest.base — contrato único dos destinos.

Mesma ideia do `LLMClient` do padrão de integração: a subclasse implementa APENAS
o transporte (`send`). Descobrir arquivos, controlar posição, lotear e repetir é
responsabilidade do coletor e vale igual para todos os destinos.
"""

from __future__ import annotations

import datetime
from dataclasses import dataclass, field


class LogPushError(Exception):
    """Falha de envio. A mensagem NUNCA inclui credencial."""


@dataclass
class Mensagem:
    """Uma unidade de envio já lida do arquivo."""
    origem: str
    texto: str                                  # linha original, sem alteração
    quando: datetime.datetime
    severidade: str = "INFO"
    msgid: str = ""
    campos: dict = field(default_factory=dict)


class LogDestination:
    """Contrato. Subclasse implementa só `send`."""

    def __init__(self, cfg: dict) -> None:
        self.cfg = cfg or {}

    def send(self, mensagens: list[Mensagem]) -> None:
        """Entrega as mensagens. Levanta LogPushError se não conseguir.

        Retornar sem exceção significa ENTREGUE — é o que autoriza o coletor a
        avançar o ponteiro.
        """
        raise NotImplementedError("destino deve implementar send()")

    def testar(self) -> str:
        """Envia uma prova de conexão. Devolve descrição curta do resultado."""
        agora = datetime.datetime.now(datetime.UTC)
        self.send([Mensagem(origem="teste", texto="Argus — teste de conexão do logpush",
                            quando=agora, severidade="INFO", msgid="LOGPUSH_TEST")])
        return "envio de teste concluído"


_REGISTRY: dict = {}


def registrar(nome: str):
    def _wrap(cls):
        _REGISTRY[nome] = cls
        return cls
    return _wrap


def criar(cfg: dict) -> LogDestination:
    nome = str((cfg or {}).get("destino", "") or "").strip()
    cls = _REGISTRY.get(nome)
    if cls is None:
        raise LogPushError(f"destino desconhecido: {nome or '(vazio)'}")
    return cls(cfg)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_base -v`
Expected: PASS (4 testes)

- [ ] **Step 5: Commit**

```bash
git add core/logpush_dest/ tests/test_logpush_base.py
git commit -m "feat(logpush): contrato LogDestination + registry"
```

---

### Task 3: Parser e formatação legível

**Files:**
- Create: `core/logpush_fmt.py`
- Test: `tests/test_logpush_fmt.py`

**Interfaces:**
- Consumes: `Mensagem` de `logpush_dest.base` (Task 2).
- Produces: `parse_rfc5424(linha: str, origem: str) -> Mensagem | None`, `para_chat(m: Mensagem, plataforma: str) -> dict`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_fmt.py`:

```python
import unittest

import sys
sys.path.insert(0, "core")
import logpush_fmt as F

LINHA = ('<130>1 2026-08-15T13:35:47.812Z PROMETEUS monitor 4821 PORT_NEW '
         '[origin@32473 run_id="a3f2" campanha="RIOCARD" ip="104.18.9.141" port="443" '
         'service="https" risk="CRITICO" asn="Cloudflare, Inc."] Nova porta [CRITICO]: 104.18.9.141:443')

AUDIT = ('<109>1 2026-08-13T22:54:20.649Z PROMETEUS argus-audit 30880 AUTHZ_DENY '
         '[argus@32473 actor="monitor" src_ip="127.0.0.1" action="scan_start" outcome="deny"] '
         'acao negada: header CSRF ausente')


class TestParse(unittest.TestCase):
    def test_extrai_campos(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        self.assertEqual(m.msgid, "PORT_NEW")
        self.assertEqual(m.campos["ip"], "104.18.9.141")
        self.assertEqual(m.campos["campanha"], "RIOCARD")
        self.assertEqual(m.texto, LINHA)

    def test_severidade_vem_do_prival(self):
        # 130 = 16*8 + 2 (CRIT) -> CRITICO
        self.assertEqual(F.parse_rfc5424(LINHA, "monitor").severidade, "CRITICO")

    def test_audit_tambem_parseia(self):
        m = F.parse_rfc5424(AUDIT, "audit")
        self.assertEqual(m.msgid, "AUTHZ_DENY")
        self.assertEqual(m.campos["actor"], "monitor")

    def test_linha_invalida_devolve_none(self):
        self.assertIsNone(F.parse_rfc5424("isso nao e syslog", "monitor"))
        self.assertIsNone(F.parse_rfc5424("", "monitor"))


class TestFormatoChat(unittest.TestCase):
    def test_google_chat_usa_text(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        p = F.para_chat(m, "google_chat")
        self.assertIn("text", p)
        self.assertIn("104.18.9.141", p["text"])
        self.assertIn("RIOCARD", p["text"])

    def test_discord_usa_content(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        self.assertIn("content", F.para_chat(m, "discord"))

    def test_telegram_usa_text_e_parse_mode(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        p = F.para_chat(m, "telegram")
        self.assertIn("text", p)

    def test_nao_vaza_linha_crua(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        texto = F.para_chat(m, "google_chat")["text"]
        self.assertNotIn("<130>1", texto)
        self.assertNotIn("origin@32473", texto)

    def test_generico_leva_campos(self):
        m = F.parse_rfc5424(LINHA, "monitor")
        p = F.para_chat(m, "generico")
        self.assertEqual(p["severidade"], "CRITICO")
        self.assertEqual(p["msgid"], "PORT_NEW")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_fmt -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush_fmt'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush_fmt.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush_fmt — RFC 5424 para leitura humana.

O bucket recebe a linha original; o chat recebe isto. Linha de syslog crua num
chat é ilegível, e alerta que ninguém lê não serve para nada.
"""

from __future__ import annotations

import datetime
import re

from logpush_dest.base import Mensagem

# <PRI>1 TIMESTAMP HOST APP PID MSGID [SD-ID campo="valor" ...] mensagem
_RE = re.compile(
    r'^<(?P<pri>\d{1,3})>1 (?P<ts>\S+) (?P<host>\S+) (?P<app>\S+) (?P<pid>\S+) '
    r'(?P<msgid>\S+) \[(?P<sdid>[^\s\]]+)(?P<sd>[^\]]*)\] ?(?P<msg>.*)$'
)
_CAMPO = re.compile(r'(\w+)="((?:[^"\\]|\\.)*)"')

# severity do syslog (prival % 8) -> vocabulário de risco do Argus
_SEV = {2: "CRITICO", 3: "CRITICO", 4: "ALTO", 5: "MEDIO", 6: "BAIXO", 7: "BAIXO"}

_EMOJI = {"CRITICO": "🔴", "ALTO": "🟠", "MEDIO": "🟡", "BAIXO": "🟢", "INFO": "🔵"}

_TITULO = {
    "PORT_NEW":    "Nova porta exposta",
    "PORT_FIXED":  "Porta fechada",
    "HOST_NEW":    "Novo subdomínio",
    "HOST_GONE":   "Subdomínio removido",
    "CRED_LEAK":   "Credenciais expostas",
    "EMAIL_RISK":  "Postura de e-mail frágil",
    "TYPO_NEW":    "Domínio sósia registrado",
    "SCAN_START":  "Scan iniciado",
    "SCAN_END":    "Scan concluído",
    "SCAN_ERR":    "Falha no scan",
    "AUTHZ_DENY":  "Acesso negado",
    "LOGPUSH_TEST": "Teste de conexão",
}

# Campos mostrados no chat, na ordem. O resto fica só no log original.
_DETALHE = ("campanha", "ip", "port", "service", "hostname", "domain",
            "asn", "risk", "actor", "src_ip", "action", "outcome")


def _desescapar(v: str) -> str:
    return v.replace('\\"', '"').replace("\\\\", "\\").replace("\\]", "]")


def parse_rfc5424(linha: str, origem: str) -> Mensagem | None:
    """Converte uma linha RFC 5424 em Mensagem. Devolve None se não casar."""
    m = _RE.match((linha or "").strip())
    if not m:
        return None
    campos = {k: _desescapar(v) for k, v in _CAMPO.findall(m.group("sd") or "")}
    try:
        quando = datetime.datetime.strptime(m.group("ts")[:19], "%Y-%m-%dT%H:%M:%S")
    except ValueError:
        quando = datetime.datetime.now()
    sev = _SEV.get(int(m.group("pri")) % 8, "INFO")
    # O risco declarado no evento manda sobre a severidade do transporte.
    risco = (campos.get("risk") or "").upper()
    if risco in ("CRITICO", "ALTO", "MEDIO", "BAIXO", "INFO"):
        sev = risco
    return Mensagem(origem=origem, texto=linha.rstrip("\n"), quando=quando,
                    severidade=sev, msgid=m.group("msgid"), campos=campos)


def _corpo(m: Mensagem) -> str:
    titulo = _TITULO.get(m.msgid, m.msgid.replace("_", " ").title())
    linhas = [f"{_EMOJI.get(m.severidade, '🔵')} {m.severidade} · {titulo}"]
    detalhes = [f"{k}: {m.campos[k]}" for k in _DETALHE if m.campos.get(k)]
    if detalhes:
        linhas.append(" · ".join(detalhes))
    corpo_msg = m.campos.get("msg") or ""
    if corpo_msg:
        linhas.append(corpo_msg)
    linhas.append(m.quando.strftime("%d/%m/%Y %H:%M"))
    return "\n".join(linhas)


def para_chat(m: Mensagem, plataforma: str) -> dict:
    """Payload pronto para a plataforma escolhida."""
    texto = _corpo(m)
    p = (plataforma or "google_chat").strip()
    if p == "discord":
        return {"content": texto[:1900]}
    if p == "teams":
        return {"@type": "MessageCard", "@context": "https://schema.org/extensions",
                "summary": "Argus", "text": texto.replace("\n", "\n\n")}
    if p == "telegram":
        return {"text": texto[:4000], "disable_web_page_preview": True}
    if p == "generico":
        return {"origem": m.origem, "severidade": m.severidade, "msgid": m.msgid,
                "quando": m.quando.isoformat(), "campos": m.campos, "texto": texto}
    # google_chat e slack usam o mesmo campo
    return {"text": texto[:3900]}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_fmt -v`
Expected: PASS (9 testes)

- [ ] **Step 5: Commit**

```bash
git add core/logpush_fmt.py tests/test_logpush_fmt.py
git commit -m "feat(logpush): parser RFC5424 e formatacao por plataforma"
```

---

### Task 4: Destino S3

**Files:**
- Create: `core/logpush_dest/s3.py`
- Test: `tests/test_logpush_s3.py`

**Interfaces:**
- Consumes: `LogDestination`, `Mensagem`, `LogPushError` (Task 2).
- Produces: `S3Destination` registrado como `"s3"`; método interno `_chave(m: Mensagem, usados: set) -> str`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_s3.py`:

```python
import datetime
import unittest

import sys
sys.path.insert(0, "core")
from logpush_dest import base as B
from logpush_dest import s3 as S3


class FakeS3:
    def __init__(self):
        self.objetos = {}

    def put_object(self, Bucket, Key, Body, **kw):
        if Key in self.objetos:
            raise AssertionError(f"sobrescrita de {Key}")
        self.objetos[Key] = Body


def msg(seg=47, texto="linha"):
    return B.Mensagem(origem="monitor", texto=texto,
                      quando=datetime.datetime(2026, 8, 15, 13, 35, seg),
                      severidade="CRITICO", msgid="PORT_NEW", campos={})


class TestChave(unittest.TestCase):
    def setUp(self):
        self.d = S3.S3Destination({"destino": "s3", "s3_bucket": "b",
                                   "s3_prefixo": "logs/argus"})
        self.d._cliente = FakeS3()

    def test_layout_do_caminho(self):
        self.d.send([msg()])
        chave = next(iter(self.d._cliente.objetos))
        self.assertEqual(chave, "logs/argus/monitor/15-08-2026-13-35-47.log")

    def test_mesmo_segundo_nao_sobrescreve(self):
        self.d.send([msg(), msg(), msg()])
        chaves = sorted(self.d._cliente.objetos)
        self.assertEqual(len(chaves), 3)
        self.assertEqual(chaves[0], "logs/argus/monitor/15-08-2026-13-35-47.log")
        self.assertEqual(chaves[1], "logs/argus/monitor/15-08-2026-13-35-47-002.log")
        self.assertEqual(chaves[2], "logs/argus/monitor/15-08-2026-13-35-47-003.log")

    def test_conteudo_e_a_linha_original(self):
        self.d.send([msg(texto="<130>1 linha crua")])
        corpo = next(iter(self.d._cliente.objetos.values()))
        self.assertEqual(corpo, b"<130>1 linha crua\n")


class TestFalha(unittest.TestCase):
    def test_erro_do_cliente_vira_logpusherror(self):
        class Quebrado:
            def put_object(self, **kw):
                raise RuntimeError("sem rede")

        d = S3.S3Destination({"destino": "s3", "s3_bucket": "b"})
        d._cliente = Quebrado()
        with self.assertRaises(B.LogPushError):
            d.send([msg()])

    def test_sem_bucket_levanta(self):
        d = S3.S3Destination({"destino": "s3"})
        with self.assertRaises(B.LogPushError):
            d.send([msg()])


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_s3 -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush_dest.s3'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush_dest/s3.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush_dest.s3 — envio para bucket S3 (ou compatível).

Objetos são WRITE-ONCE: cada mensagem vira um objeto que nunca é reescrito. O
`pull-logs-s3` decide o que baixar por LastModified, então reescrever um objeto
faria o coletor rebaixar tudo e duplicar no SIEM.
"""

from __future__ import annotations

from .base import LogDestination, LogPushError, Mensagem, registrar


@registrar("s3")
class S3Destination(LogDestination):
    """Um objeto por mensagem, nome com timestamp e sufixo anticolisão."""

    def __init__(self, cfg: dict) -> None:
        super().__init__(cfg)
        self._cliente = None            # injetável nos testes

    def _obter_cliente(self):
        if self._cliente is not None:
            return self._cliente
        try:
            import boto3
        except ImportError as exc:
            raise LogPushError("boto3 não instalado (apt install python3-boto3)") from exc
        extra = {}
        if self.cfg.get("s3_endpoint"):
            extra["endpoint_url"] = self.cfg["s3_endpoint"]
        chave = self.cfg.get("s3_access_key") or None
        segredo = self.cfg.get("s3_secret_key") or None
        try:
            sessao = boto3.session.Session(
                aws_access_key_id=chave, aws_secret_access_key=segredo,
                region_name=self.cfg.get("s3_regiao") or "us-east-1")
            self._cliente = sessao.client("s3", **extra)
        except Exception as exc:
            raise LogPushError(f"não consegui criar o cliente S3: {type(exc).__name__}") from exc
        return self._cliente

    def _chave(self, m: Mensagem, usados: set) -> str:
        prefixo = str(self.cfg.get("s3_prefixo") or "logs/argus").strip("/")
        carimbo = m.quando.strftime("%d-%m-%Y-%H-%M-%S")
        base = f"{prefixo}/{m.origem}/{carimbo}"
        chave = f"{base}.log"
        n = 1
        # O segundo NÃO é único: vários eventos caem no mesmo carimbo. Sem o
        # sufixo o S3 sobrescreveria em silêncio e o evento sumiria sem erro.
        while chave in usados:
            n += 1
            chave = f"{base}-{n:03d}.log"
        usados.add(chave)
        return chave

    def send(self, mensagens: list[Mensagem]) -> None:
        if not mensagens:
            return
        bucket = str(self.cfg.get("s3_bucket") or "").strip()
        if not bucket:
            raise LogPushError("bucket não configurado")
        cliente = self._obter_cliente()
        usados: set = set()
        for m in mensagens:
            chave = self._chave(m, usados)
            corpo = (m.texto.rstrip("\n") + "\n").encode("utf-8", "replace")
            try:
                cliente.put_object(Bucket=bucket, Key=chave, Body=corpo)
            except Exception as exc:
                # Sem credencial no texto do erro.
                raise LogPushError(
                    f"falha ao gravar {chave}: {type(exc).__name__}") from exc
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_s3 -v`
Expected: PASS (5 testes)

- [ ] **Step 5: Commit**

```bash
git add core/logpush_dest/s3.py tests/test_logpush_s3.py
git commit -m "feat(logpush): destino S3 write-once com sufixo anticolisao"
```

---

### Task 5: Destino Webhook

**Files:**
- Create: `core/logpush_dest/webhook.py`
- Test: `tests/test_logpush_webhook.py`

**Interfaces:**
- Consumes: `LogDestination`, `Mensagem`, `LogPushError` (Task 2); `para_chat` (Task 3); `url_segura`, `severidades_ligadas` (Task 1).
- Produces: `WebhookDestination` registrado como `"webhook"`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_webhook.py`:

```python
import datetime
import unittest

import sys
sys.path.insert(0, "core")
from logpush_dest import base as B
from logpush_dest import webhook as W


class FakeResp:
    def __init__(self, status=200, texto="ok"):
        self.status_code = status
        self.text = texto


def msg(sev="CRITICO"):
    return B.Mensagem(origem="monitor", texto="linha",
                      quando=datetime.datetime(2026, 8, 15, 13, 35, 47),
                      severidade=sev, msgid="PORT_NEW",
                      campos={"ip": "1.2.3.4", "campanha": "RIOCARD"})


class TestFiltroSeveridade(unittest.TestCase):
    def _destino(self, **cfg):
        base = {"destino": "webhook", "webhook_url": "https://exemplo.com/hook",
                "webhook_plataforma": "google_chat",
                "sev_CRITICO": True, "sev_ALTO": True}
        base.update(cfg)
        d = W.WebhookDestination(base)
        d._enviadas = []
        d._post = lambda url, payload: (d._enviadas.append(payload), FakeResp())[1]
        return d

    def test_envia_severidade_marcada(self):
        d = self._destino()
        d.send([msg("CRITICO")])
        self.assertEqual(len(d._enviadas), 1)

    def test_descarta_severidade_nao_marcada(self):
        d = self._destino()
        d.send([msg("BAIXO")])
        self.assertEqual(d._enviadas, [])

    def test_todas_marcadas_envia_tudo(self):
        d = self._destino(sev_MEDIO=True, sev_BAIXO=True, sev_INFO=True)
        d.send([msg("CRITICO"), msg("BAIXO"), msg("INFO")])
        self.assertEqual(len(d._enviadas), 3)


class TestPayload(unittest.TestCase):
    def test_usa_formato_da_plataforma(self):
        d = W.WebhookDestination({"destino": "webhook", "webhook_plataforma": "discord",
                                  "webhook_url": "https://exemplo.com/h", "sev_CRITICO": True})
        d._enviadas = []
        d._post = lambda url, payload: (d._enviadas.append(payload), FakeResp())[1]
        d.send([msg()])
        self.assertIn("content", d._enviadas[0])


class TestSeguranca(unittest.TestCase):
    def test_url_insegura_levanta(self):
        d = W.WebhookDestination({"destino": "webhook", "webhook_url": "http://10.0.0.1/h",
                                  "sev_CRITICO": True})
        with self.assertRaises(B.LogPushError):
            d.send([msg()])

    def test_erro_http_vira_logpusherror(self):
        d = W.WebhookDestination({"destino": "webhook", "webhook_url": "https://exemplo.com/h",
                                  "sev_CRITICO": True})
        d._post = lambda url, payload: FakeResp(500, "erro interno")
        with self.assertRaises(B.LogPushError):
            d.send([msg()])

    def test_erro_nao_vaza_url(self):
        d = W.WebhookDestination({"destino": "webhook",
                                  "webhook_url": "https://exemplo.com/hook/SEGREDO123",
                                  "sev_CRITICO": True})
        d._post = lambda url, payload: FakeResp(403, "proibido")
        try:
            d.send([msg()])
        except B.LogPushError as e:
            self.assertNotIn("SEGREDO123", str(e))
        else:
            self.fail("deveria ter levantado")


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_webhook -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush_dest.webhook'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush_dest/webhook.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush_dest.webhook — alerta legível em chat (Google Chat, Slack, Discord,
Teams, Telegram ou endpoint próprio).

Só as severidades marcadas saem: mandar tudo para um chat vira ruído e a
plataforma passa a descartar por limite de taxa.
"""

from __future__ import annotations

import requests

from logpush_config import severidades_ligadas, url_segura
from logpush_fmt import para_chat

from .base import LogDestination, LogPushError, Mensagem, registrar

_TIMEOUT = 15


@registrar("webhook")
class WebhookDestination(LogDestination):

    def _post(self, url: str, payload: dict):
        return requests.post(url, json=payload, timeout=_TIMEOUT)

    def send(self, mensagens: list[Mensagem]) -> None:
        if not mensagens:
            return
        url = str(self.cfg.get("webhook_url") or "").strip()
        if not url_segura(url):
            # Não repete a URL no erro: ela é segredo (quem tem a URL posta no canal).
            raise LogPushError("URL de webhook inválida: exige https e host público")
        plataforma = str(self.cfg.get("webhook_plataforma") or "google_chat").strip()
        permitidas = set(severidades_ligadas(self.cfg))
        if plataforma == "telegram":
            chat_id = str(self.cfg.get("webhook_chat_id") or "").strip()
            if not chat_id:
                raise LogPushError("Telegram exige chat_id")
        for m in mensagens:
            if m.severidade not in permitidas:
                continue
            payload = para_chat(m, plataforma)
            if plataforma == "telegram":
                payload["chat_id"] = str(self.cfg.get("webhook_chat_id") or "")
            try:
                resp = self._post(url, payload)
            except Exception as exc:
                raise LogPushError(f"falha de rede no webhook: {type(exc).__name__}") from exc
            if resp.status_code >= 300:
                corpo = (getattr(resp, "text", "") or "")[:120]
                raise LogPushError(f"webhook respondeu HTTP {resp.status_code}: {corpo}")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_webhook -v`
Expected: PASS (7 testes)

- [ ] **Step 5: Commit**

```bash
git add core/logpush_dest/webhook.py tests/test_logpush_webhook.py
git commit -m "feat(logpush): destino webhook com filtro de severidade"
```

---

### Task 6: Coletor com ponteiro e rotação

**Files:**
- Create: `core/logpush.py`
- Test: `tests/test_logpush_coletor.py`

**Interfaces:**
- Consumes: `ler`, `origens_ligadas`, `POR_ID`, `base_dir` (Task 1); `criar`, `Mensagem`, `LogPushError` (Task 2); `parse_rfc5424` (Task 3).
- Produces: `estado_path() -> Path`, `ler_estado() -> dict`, `gravar_estado(e: dict) -> None`, `coletar(cfg, raiz) -> list[Mensagem]`, `executar(cfg=None) -> dict`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_coletor.py`:

```python
import os
import tempfile
import unittest
from pathlib import Path

import sys
sys.path.insert(0, "core")
import logpush as LP

LINHA = ('<130>1 2026-08-15T13:35:47.812Z HOST monitor 1 PORT_NEW '
         '[origin@32473 ip="1.2.3.4" risk="CRITICO"] Nova porta')


class Base(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        self.raiz = Path(self.tmp.name) / "log"
        (self.raiz / "monitor").mkdir(parents=True)
        os.environ["ARGUS_BASE"] = self.tmp.name
        self.arq = self.raiz / "monitor" / "monitor.log"
        self.cfg = {"origem_monitor": True}

    def tearDown(self):
        self.tmp.cleanup()

    def escrever(self, n=1):
        with open(self.arq, "a", encoding="utf-8", newline="") as fh:
            for _ in range(n):
                fh.write(LINHA + "\n")


class TestPonteiro(Base):
    def test_le_so_o_que_e_novo(self):
        self.escrever(2)
        self.assertEqual(len(LP.coletar(self.cfg, self.raiz)), 2)
        LP.gravar_estado(LP._estado_apos(self.cfg, self.raiz))
        self.assertEqual(len(LP.coletar(self.cfg, self.raiz)), 0)
        self.escrever(1)
        self.assertEqual(len(LP.coletar(self.cfg, self.raiz)), 1)

    def test_origem_desligada_nao_le(self):
        self.escrever(2)
        self.assertEqual(LP.coletar({"origem_monitor": False}, self.raiz), [])


class TestRotacao(Base):
    def test_rotacao_nao_perde_nem_duplica(self):
        self.escrever(2)
        LP.coletar(self.cfg, self.raiz)
        LP.gravar_estado(LP._estado_apos(self.cfg, self.raiz))
        # logrotate: renomeia e cria arquivo novo (inode diferente)
        self.arq.rename(self.raiz / "monitor" / "monitor.log.1")
        self.escrever(3)
        msgs = LP.coletar(self.cfg, self.raiz)
        self.assertEqual(len(msgs), 3)


class TestTruncamento(Base):
    def test_arquivo_menor_que_ponteiro_recomeca(self):
        self.escrever(5)
        LP.coletar(self.cfg, self.raiz)
        LP.gravar_estado(LP._estado_apos(self.cfg, self.raiz))
        with open(self.arq, "w", encoding="utf-8", newline="") as fh:
            fh.write(LINHA + "\n")
        self.assertEqual(len(LP.coletar(self.cfg, self.raiz)), 1)


class TestFalhaNaoAvanca(Base):
    def test_ponteiro_nao_avanca_se_destino_falha(self):
        self.escrever(2)
        antes = LP.ler_estado()
        cfg = dict(self.cfg, destino="inexistente")
        resultado = LP.executar(cfg, raiz=self.raiz)
        self.assertFalse(resultado["ok"])
        self.assertEqual(LP.ler_estado(), antes)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_coletor -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'logpush'`

- [ ] **Step 3: Write minimal implementation**

Create `core/logpush.py`:

```python
#!/usr/bin/env python3
#
# Argus ASM — monitoramento de superfície de ataque
# Copyright (C) 2026  Bruno Santos
#
# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program.  If not, see <https://www.gnu.org/licenses/>.

"""
logpush — lê o que há de novo nos logs e entrega ao destino configurado.

O ponteiro guarda (inode, posição) por arquivo e só avança DEPOIS que o destino
confirma. Falha de rede vira atraso, nunca buraco: o ciclo seguinte retoma do
mesmo ponto.

Uso: python3 logpush.py
"""

from __future__ import annotations

import datetime
import json
import os
import sys
from pathlib import Path

import logpush_config as LC
from logpush_dest import s3 as _s3          # noqa: F401 - registra o destino
from logpush_dest import webhook as _wh     # noqa: F401 - registra o destino
from logpush_dest.base import LogPushError, Mensagem, criar
from logpush_fmt import parse_rfc5424

RAIZ_LOG = Path(os.environ.get("ARGUS_LOG_ROOT", "/var/log/argus"))
MAX_POR_CICLO = int(os.environ.get("ARGUS_LOGPUSH_MAX", "5000"))


def estado_path() -> Path:
    return LC.base_dir() / "store" / "logpush_state.json"


def ler_estado() -> dict:
    try:
        return json.loads(estado_path().read_text(encoding="utf-8"))
    except Exception:
        return {}


def gravar_estado(estado: dict) -> None:
    caminho = estado_path()
    try:
        caminho.parent.mkdir(parents=True, exist_ok=True)
        with open(caminho, "w", encoding="utf-8", newline="") as fh:
            json.dump(estado, fh, ensure_ascii=False, indent=2)
            fh.flush()
            os.fsync(fh.fileno())
    except OSError as exc:
        print(f"[LOGPUSH] não consegui gravar o estado: {exc}", file=sys.stderr)


def _arquivos(cfg: dict, raiz: Path) -> list[tuple[str, Path, bool]]:
    """(origem, caminho, estruturado) de cada arquivo das origens ligadas."""
    saida = []
    for oid in LC.origens_ligadas(cfg):
        origem = LC.POR_ID[oid]
        padrao = origem["caminho"]
        if "*" in padrao:
            achados = sorted((raiz / padrao.split("/")[0]).glob(padrao.split("/")[1]))
        else:
            p = raiz / padrao
            achados = [p] if p.exists() else []
        for a in achados:
            saida.append((oid, a, origem["estruturado"]))
    return saida


def _ler_novo(caminho: Path, marca: dict) -> tuple[str, int, int]:
    """Devolve (conteúdo novo, inode atual, posição final)."""
    st = caminho.stat()
    inode, tamanho = st.st_ino, st.st_size
    pos = int(marca.get("pos", 0)) if int(marca.get("inode", 0)) == inode else 0
    if pos > tamanho:
        pos = 0            # truncado (ex.: scan/*.log é reescrito a cada execução)
    with open(caminho, "r", encoding="utf-8", errors="replace") as fh:
        fh.seek(pos)
        conteudo = fh.read()
        fim = fh.tell()
    return conteudo, inode, fim


def _pendente_rotacionado(caminho: Path, marca: dict) -> str:
    """Resto do arquivo anterior quando o logrotate criou um novo (inode mudou).

    O logrotate usa `create` + `delaycompress`: o anterior fica como `.log.1`
    ainda descomprimido, então dá para terminar de ler antes de seguir.
    """
    if not marca:
        return ""
    anterior = caminho.with_suffix(caminho.suffix + ".1")
    if not anterior.exists():
        return ""
    try:
        if anterior.stat().st_ino != int(marca.get("inode", 0)):
            return ""
        with open(anterior, "r", encoding="utf-8", errors="replace") as fh:
            fh.seek(int(marca.get("pos", 0)))
            return fh.read()
    except OSError:
        return ""


def coletar(cfg: dict, raiz: Path | None = None) -> list[Mensagem]:
    """Mensagens novas desde o ponteiro. NÃO altera o estado."""
    raiz = raiz or RAIZ_LOG
    estado = ler_estado()
    mensagens: list[Mensagem] = []
    for origem, caminho, estruturado in _arquivos(cfg, raiz):
        chave = str(caminho.relative_to(raiz))
        marca = estado.get(chave, {})
        try:
            st = caminho.stat()
        except OSError:
            continue
        bruto = ""
        if marca and int(marca.get("inode", 0)) != st.st_ino:
            bruto += _pendente_rotacionado(caminho, marca)
        try:
            novo, _inode, _fim = _ler_novo(caminho, marca)
        except OSError:
            continue
        bruto += novo
        if not bruto.strip():
            continue
        if estruturado:
            for linha in bruto.splitlines():
                if not linha.strip():
                    continue
                m = parse_rfc5424(linha, origem)
                if m is not None:
                    mensagens.append(m)
                if len(mensagens) >= MAX_POR_CICLO:
                    return mensagens
        else:
            # Saída de execução: o arquivo inteiro é uma mensagem.
            mensagens.append(Mensagem(
                origem=origem, texto=bruto.rstrip("\n"),
                quando=datetime.datetime.now(), severidade="INFO",
                msgid="SCAN_OUTPUT", campos={"arquivo": caminho.name}))
    return mensagens


def _estado_apos(cfg: dict, raiz: Path | None = None) -> dict:
    """Estado que reflete tudo lido até agora."""
    raiz = raiz or RAIZ_LOG
    estado = dict(ler_estado())
    agora = datetime.datetime.now(datetime.UTC).strftime("%Y-%m-%dT%H:%M:%SZ")
    for _origem, caminho, _e in _arquivos(cfg, raiz):
        try:
            st = caminho.stat()
        except OSError:
            continue
        estado[str(caminho.relative_to(raiz))] = {
            "inode": st.st_ino, "pos": st.st_size, "enviado_em": agora}
    return estado


def executar(cfg: dict | None = None, raiz: Path | None = None) -> dict:
    """Um ciclo: coleta, envia e — só se o envio der certo — avança o ponteiro."""
    cfg = cfg if cfg is not None else LC.ler()
    raiz = raiz or RAIZ_LOG
    if not cfg.get("logpush_ligado", False) and cfg is not None and "destino" not in cfg:
        return {"ok": True, "enviadas": 0, "detalhe": "logpush desligado"}
    mensagens = coletar(cfg, raiz)
    if not mensagens:
        return {"ok": True, "enviadas": 0, "detalhe": "nada novo"}
    try:
        destino = criar(cfg)
        destino.send(mensagens)
    except LogPushError as exc:
        # Ponteiro NÃO avança: o próximo ciclo reenvia o mesmo trecho.
        print(f"[LOGPUSH] envio falhou: {exc}", file=sys.stderr)
        return {"ok": False, "enviadas": 0, "detalhe": str(exc)}
    gravar_estado(_estado_apos(cfg, raiz))
    print(f"[LOGPUSH] {len(mensagens)} mensagem(ns) enviada(s)")
    return {"ok": True, "enviadas": len(mensagens), "detalhe": ""}


def main() -> int:
    resultado = executar()
    return 0 if resultado["ok"] else 1


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_coletor -v`
Expected: PASS (5 testes)

- [ ] **Step 5: Run the whole suite and lint**

Run: `python3 -m unittest discover -s tests -v && ruff check .`
Expected: todos passam, `All checks passed!`

- [ ] **Step 6: Commit**

```bash
git add core/logpush.py tests/test_logpush_coletor.py
git commit -m "feat(logpush): coletor com ponteiro por inode e rotacao"
```

---

### Task 7: API no webapp

**Files:**
- Modify: `core/webapp.py` (adicionar rotas junto às demais `@app.get`/`@app.post`)
- Test: `tests/test_logpush_api.py`

**Interfaces:**
- Consumes: `logpush_config` (Task 1), `logpush` (Task 6), `criar` (Task 2).
- Produces: `GET /api/logpush`, `POST /api/logpush`, `POST /api/logpush/test`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_api.py`:

```python
import os
import sys
import tempfile
import types
import unittest

sys.modules.setdefault("nmap", types.ModuleType("nmap"))
sys.path.insert(0, "core")


class TestAPI(unittest.TestCase):
    def setUp(self):
        self.tmp = tempfile.TemporaryDirectory()
        os.environ["ARGUS_BASE"] = self.tmp.name
        os.environ["ARGUS_DB"] = os.path.join(self.tmp.name, "store", "argus.db")
        os.makedirs(os.path.join(self.tmp.name, "store"), exist_ok=True)
        import webapp
        self.app = webapp.create_app().test_client()
        self.H = {"X-Requested-With": "argus", "X-Remote-User": "monitor"}

    def tearDown(self):
        self.tmp.cleanup()

    def test_get_devolve_catalogo(self):
        r = self.app.get("/api/logpush", headers=self.H)
        self.assertEqual(r.status_code, 200)
        j = r.get_json()
        self.assertIn("origens", j)
        self.assertEqual(len(j["origens"]), 7)

    def test_post_grava_e_get_mascara_segredo(self):
        r = self.app.post("/api/logpush", headers=self.H, json={
            "destino": "s3", "s3_bucket": "meu-bucket",
            "s3_secret_key": "SEGREDOabcdefgh", "origem_monitor": True})
        self.assertEqual(r.status_code, 200)
        j = self.app.get("/api/logpush", headers=self.H).get_json()
        self.assertEqual(j["config"]["s3_bucket"], "meu-bucket")
        self.assertNotIn("SEGREDOabcdefgh", str(j))
        self.assertTrue(j["config"]["s3_secret_key"].startswith("••••"))

    def test_post_sem_csrf_recusado(self):
        r = self.app.post("/api/logpush", json={"destino": "s3"},
                          headers={"X-Remote-User": "monitor"})
        self.assertEqual(r.status_code, 403)

    def test_url_insegura_recusada(self):
        r = self.app.post("/api/logpush", headers=self.H, json={
            "destino": "webhook", "webhook_url": "http://10.0.0.1/x"})
        self.assertEqual(r.status_code, 400)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_api -v`
Expected: FAIL — 404 nas rotas `/api/logpush`

- [ ] **Step 3: Write minimal implementation**

Em `core/webapp.py`, adicionar o import junto aos existentes (perto de `import providers as PROV`):

```python
import logpush_config as LPC
```

E, logo antes de `@app.get("/api/health")`, adicionar:

```python
    @app.get("/api/logpush")
    def logpush_get():
        """Catálogo + config atual. Segredo SEMPRE mascarado."""
        cfg = dict(LPC.ler())
        seguro = {}
        for d in LPC.DESTINOS:
            for campo in d["campos"]:
                nome = campo["nome"]
                if nome not in cfg:
                    continue
                seguro[nome] = LPC.mascarar(cfg[nome]) if campo["segredo"] else cfg[nome]
        for chave, valor in cfg.items():
            if chave.startswith(("origem_", "sev_")) or chave in ("destino", "logpush_ligado"):
                seguro[chave] = valor
        try:
            import logpush as LP
            estado = {"ultimo": max((m.get("enviado_em", "") for m in LP.ler_estado().values()),
                                    default="")}
        except Exception:
            estado = {"ultimo": ""}
        return jsonify(ok=True, config=seguro, estado=estado,
                       origens=LPC.ORIGENS, destinos=LPC.DESTINOS,
                       plataformas=LPC.PLATAFORMAS, severidades=LPC.SEVERIDADES)

    @app.post("/api/logpush")
    def logpush_set():
        if not _csrf_ok():
            _audit(request, "AUTHZ_DENY", "ação negada: header CSRF ausente",
                   outcome="deny", action="logpush_set")
            return jsonify(ok=False, error="CSRF: header ausente"), 403
        dados = request.get_json(silent=True) or {}
        cfg = dict(LPC.ler())
        for chave, valor in dados.items():
            # Campo de segredo em branco = "não mudar": a interface manda o valor
            # mascarado de volta, e gravá-lo destruiria o segredo real.
            if isinstance(valor, str) and valor.startswith("••••"):
                continue
            cfg[chave] = valor
        url = str(cfg.get("webhook_url") or "").strip()
        if cfg.get("destino") == "webhook" and url and not LPC.url_segura(url):
            return jsonify(ok=False,
                           error="URL de webhook inválida: exige https e host público"), 400
        try:
            LPC.gravar(cfg)
        except OSError as exc:
            return jsonify(ok=False, error=f"sem permissão para gravar a configuração: {exc}"), 500
        _audit(request, "LOGPUSH_SET", f"configuração de logpush alterada (destino={cfg.get('destino','')})",
               outcome="success", action="logpush_set", obj="logpush", object_type="config")
        return jsonify(ok=True)

    @app.post("/api/logpush/test")
    def logpush_test():
        if not _csrf_ok():
            return jsonify(ok=False, error="CSRF: header ausente"), 403
        try:
            from logpush_dest.base import LogPushError, criar
            from logpush_dest import s3 as _s3      # noqa: F401 - registra
            from logpush_dest import webhook as _wh  # noqa: F401 - registra
            destino = criar(LPC.ler())
            detalhe = destino.testar()
        except LogPushError as exc:
            _audit(request, "LOGPUSH_TEST", f"teste de logpush falhou: {exc}",
                   outcome="failure", action="logpush_test")
            return jsonify(ok=False, error=str(exc)), 400
        except Exception as exc:
            return jsonify(ok=False, error=f"{type(exc).__name__}: {exc}"), 500
        _audit(request, "LOGPUSH_TEST", "teste de logpush bem-sucedido",
               outcome="success", action="logpush_test")
        return jsonify(ok=True, detalhe=detalhe)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_api -v`
Expected: PASS (4 testes)

- [ ] **Step 5: Verify RBAC guard covers the new routes**

Run: `python3 -m unittest tests.test_logpush_api -v && grep -n "_SELF_SERVICE" core/webapp.py`
Expected: o guard global `_authorize()` já barra POST de perfil `user` — nada a fazer, apenas confirmar que `/api/logpush` **não** está na lista de exceções.

- [ ] **Step 6: Commit**

```bash
git add core/webapp.py tests/test_logpush_api.py
git commit -m "feat(logpush): API de configuracao e teste de conexao"
```

---

### Task 8: Página web

**Files:**
- Modify: `core/reporter.py` (nova função `build_logpush_page()`, item de nav, `write_portal`)
- Test: `tests/test_logpush_page.py`

**Interfaces:**
- Consumes: API da Task 7.
- Produces: `build_logpush_page() -> str`; arquivo `logpush.html` no docroot.

- [ ] **Step 1: Write the failing test**

Create `tests/test_logpush_page.py`:

```python
import sys
import types
import unittest

sys.modules.setdefault("nmap", types.ModuleType("nmap"))
sys.path.insert(0, "core")
import reporter


class TestPagina(unittest.TestCase):
    def setUp(self):
        self.html = reporter.build_logpush_page()

    def test_tem_titulo_padrao(self):
        self.assertIn("<title>Logpush — Argus</title>", self.html)

    def test_tem_checkbox_por_origem(self):
        for oid in ("monitor", "submonitor", "credentials", "email",
                    "typosquat", "audit", "scan"):
            self.assertIn(f'id="origem-{oid}"', self.html)

    def test_tem_checkbox_por_severidade(self):
        for sev in ("CRITICO", "ALTO", "MEDIO", "BAIXO", "INFO"):
            self.assertIn(f'id="sev-{sev}"', self.html)

    def test_avisa_sobre_flood(self):
        self.assertIn("flood", self.html.lower())

    def test_tem_botao_testar(self):
        self.assertIn("lp-testar", self.html)

    def test_nav_tem_logpush(self):
        self.assertIn("logpush.html", reporter.build_dashboard())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest tests.test_logpush_page -v`
Expected: FAIL — `AttributeError: module 'reporter' has no attribute 'build_logpush_page'`

- [ ] **Step 3: Write minimal implementation**

Em `core/reporter.py`:

**3a.** Adicionar o item de navegação, junto dos existentes (procure `("provedores.html", "Fontes"`), acrescentando na mesma lista:

```python
        ("logpush.html", "Logpush"),
```

**3b.** Adicionar o ícone no dicionário `_NAV_ICONS`, seguindo o padrão dos demais:

```python
    "logpush": '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" '
               'stroke-linecap="round"><path d="M12 19V5M5 12l7-7 7 7"/></svg>',
```

**3c.** Criar `build_logpush_page()` seguindo o modelo de `build_providers_page()` — mesma
estrutura de `_portal_shell`, mesmo `api()` do JS, mesmo tratamento de `data-write="1"` para o RBAC:

```python
def build_logpush_page() -> str:
    """Página de configuração do envio de logs para fora."""
    corpo = (
        '<div class="panel panel-pad" style="margin-bottom:16px">'
        f'<h2>{_NAV_ICONS["logpush"]} Envio de logs</h2>'
        '<div class="page-sub">Manda o que o Argus registra para um bucket S3 ou para um '
        'chat. Útil quando a aplicação roda fora do seu ambiente: os logs continuam aqui '
        '(o logrotate cuida deles) e uma cópia segue para onde você acompanha.</div>'
        '<label style="display:flex;gap:8px;align-items:center;margin-top:12px">'
        '<input type="checkbox" id="lp-ligado" data-write="1"> <b>Ligado</b></label>'
        '</div>'

        '<div class="panel panel-pad" style="margin-bottom:16px">'
        '<h2>O que enviar</h2><div id="lp-origens" class="lp-grid"></div></div>'

        '<div class="panel panel-pad" style="margin-bottom:16px">'
        '<h2>Para onde</h2>'
        '<label for="lp-destino" class="page-sub">Destino</label>'
        '<select id="lp-destino" data-write="1" style="max-width:100%"></select>'
        '<div id="lp-campos" style="margin-top:12px"></div>'
        '<div id="lp-sev-bloco" style="display:none;margin-top:14px">'
        '<div class="page-sub" style="margin-bottom:6px">Enviar quais severidades</div>'
        '<div id="lp-sev" class="lp-grid"></div>'
        '<div id="lp-flood" class="page-sub" style="display:none;margin-top:8px;'
        'border-left:3px solid var(--orange);padding-left:9px">'
        '⚠️ Com todas as severidades ligadas, uma execução pode disparar centenas de '
        'mensagens (flood). A maioria dos serviços de chat aplica limite de taxa e passa '
        'a descartar.</div></div>'
        '<div style="display:flex;gap:8px;margin-top:14px;flex-wrap:wrap">'
        '<button class="btn btn-pdf" type="button" id="lp-salvar" data-write="1">Salvar</button>'
        '<button class="btn" type="button" id="lp-testar" data-write="1">Testar conexão</button>'
        '</div></div>'

        '<div id="lp-msg" class="panel panel-pad"></div>'
    )
    css = (
        '<style>'
        '.lp-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(min(190px,100%),1fr));gap:8px}'
        '.lp-grid label{display:flex;gap:8px;align-items:center;font-size:13px}'
        '#lp-campos label{display:block;font-size:12px;color:var(--muted);margin-top:9px}'
        '#lp-campos input,#lp-campos select{width:100%;max-width:100%;background:var(--bg);'
        'color:var(--text);border:1px solid var(--border);border-radius:var(--radius-sm);'
        'padding:8px 10px;font-size:13px}'
        '</style>'
    )
    return _portal_shell("logpush", "Logpush", "Envio de logs para S3 ou chat", corpo + css + _LOGPUSH_JS)
```

**3d.** Criar a constante `_LOGPUSH_JS` (antes da função), no mesmo estilo do JS de provedores:

```python
_LOGPUSH_JS = r"""<script>
(function(){
  var CAT=null;
  function esc(s){return String(s==null?'':s).replace(/[&<>"]/g,function(c){
    return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c];});}
  function msg(t,tipo){var e=document.getElementById('lp-msg');
    e.innerHTML='<div class="page-sub" style="border-left:3px solid var('+
      (tipo==='err'?'--red':'--green')+');padding-left:9px">'+esc(t)+'</div>';}
  function api(url,opt){
    opt=opt||{}; opt.headers=Object.assign({'Content-Type':'application/json',
      'X-Requested-With':'argus'},opt.headers||{});
    return fetch(url,opt).then(function(r){return r.json().then(function(j){
      if(!r.ok||j.ok===false) throw new Error(j.error||('HTTP '+r.status)); return j;});});
  }
  function marcarSev(){
    var todas=CAT.severidades.every(function(s){
      var e=document.getElementById('sev-'+s); return e&&e.checked;});
    document.getElementById('lp-flood').style.display=todas?'block':'none';
  }
  function pintarCampos(cfg){
    var destino=document.getElementById('lp-destino').value;
    var d=CAT.destinos.filter(function(x){return x.id===destino;})[0];
    var host=document.getElementById('lp-campos'); host.innerHTML='';
    if(!d) return;
    d.campos.forEach(function(c){
      var v=cfg[c.nome]!==undefined?cfg[c.nome]:(c.padrao||'');
      var lab=document.createElement('label'); lab.textContent=c.label;
      lab.setAttribute('for','lp-'+c.nome);
      var inp;
      if(c.nome==='webhook_plataforma'){
        inp=document.createElement('select');
        CAT.plataformas.forEach(function(p){
          var o=document.createElement('option'); o.value=p; o.textContent=p; inp.appendChild(o);});
      } else {
        inp=document.createElement('input');
        inp.type=c.segredo?'password':'text';
        inp.placeholder=c.segredo&&v?('configurado ('+v+') — digite para trocar'):'';
        if(c.segredo) v='';
      }
      inp.id='lp-'+c.nome; inp.value=v; inp.setAttribute('data-write','1');
      host.appendChild(lab); host.appendChild(inp);
    });
    document.getElementById('lp-sev-bloco').style.display=(destino==='webhook')?'block':'none';
  }
  function carregar(){
    api('/api/logpush',{cache:'no-store'}).then(function(j){
      CAT=j; var cfg=j.config||{};
      document.getElementById('lp-ligado').checked=!!cfg.logpush_ligado;
      var og=document.getElementById('lp-origens'); og.innerHTML='';
      j.origens.forEach(function(o){
        var l=document.createElement('label');
        l.innerHTML='<input type="checkbox" data-write="1" id="origem-'+o.id+'"'+
          (cfg['origem_'+o.id]?' checked':'')+'> '+esc(o.label);
        og.appendChild(l);
      });
      var sg=document.getElementById('lp-sev'); sg.innerHTML='';
      j.severidades.forEach(function(s){
        var marcado=(cfg['sev_'+s]!==undefined)?cfg['sev_'+s]:(s==='CRITICO'||s==='ALTO');
        var l=document.createElement('label');
        l.innerHTML='<input type="checkbox" data-write="1" id="sev-'+s+'"'+
          (marcado?' checked':'')+'> '+esc(s);
        sg.appendChild(l);
        l.querySelector('input').addEventListener('change',marcarSev);
      });
      var sel=document.getElementById('lp-destino'); sel.innerHTML='';
      j.destinos.forEach(function(d){
        var o=document.createElement('option'); o.value=d.id; o.textContent=d.label;
        sel.appendChild(o);});
      sel.value=cfg.destino||'s3';
      sel.onchange=function(){pintarCampos(cfg);};
      pintarCampos(cfg); marcarSev();
      if(j.estado&&j.estado.ultimo) msg('Último envio: '+j.estado.ultimo);
    }).catch(function(e){msg(e.message,'err');});
  }
  function salvar(){
    var corpo={logpush_ligado:document.getElementById('lp-ligado').checked,
               destino:document.getElementById('lp-destino').value};
    CAT.origens.forEach(function(o){
      corpo['origem_'+o.id]=document.getElementById('origem-'+o.id).checked;});
    CAT.severidades.forEach(function(s){
      corpo['sev_'+s]=document.getElementById('sev-'+s).checked;});
    var d=CAT.destinos.filter(function(x){return x.id===corpo.destino;})[0];
    d.campos.forEach(function(c){
      var e=document.getElementById('lp-'+c.nome);
      if(e&&e.value!=='') corpo[c.nome]=e.value;});
    api('/api/logpush',{method:'POST',body:JSON.stringify(corpo)})
      .then(function(){msg('Configuração salva.'); carregar();})
      .catch(function(e){msg(e.message,'err');});
  }
  function testar(){
    msg('Enviando mensagem de teste…');
    api('/api/logpush/test',{method:'POST',body:'{}'})
      .then(function(j){msg('Teste OK — '+(j.detalhe||'enviado'));})
      .catch(function(e){msg('Teste falhou: '+e.message,'err');});
  }
  document.addEventListener('DOMContentLoaded',function(){
    document.getElementById('lp-salvar').addEventListener('click',salvar);
    document.getElementById('lp-testar').addEventListener('click',testar);
    carregar();
  });
})();
</script>"""
```

**3e.** Em `write_portal()`, junto das demais páginas:

```python
    (d / "logpush.html").write_text(build_logpush_page(), encoding="utf-8")
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest tests.test_logpush_page -v`
Expected: PASS (6 testes)

- [ ] **Step 5: Check responsiveness at 320px**

Gerar a página e conferir que não estoura, como nas demais:

```bash
python3 -c "
import sys, types, io
sys.path.insert(0,'core'); sys.modules.setdefault('nmap', types.ModuleType('nmap'))
import reporter
io.open('/tmp/logpush.html','w',encoding='utf-8',newline='').write(reporter.build_logpush_page())
io.open('/tmp/app.css','w',encoding='utf-8',newline='').write(reporter.app_css())
print('gerado')
"
```

Expected: servir localmente e confirmar `scrollWidth == clientWidth` a 320px (o `.lp-grid` usa `minmax(min(190px,100%),1fr)` justamente para isso).

- [ ] **Step 6: Commit**

```bash
git add core/reporter.py tests/test_logpush_page.py
git commit -m "feat(logpush): pagina de configuracao na interface"
```

---

### Task 9: Instalação e agendamento

**Files:**
- Modify: `install.sh`
- Modify: `VERSION`

**Interfaces:**
- Consumes: todos os módulos anteriores.
- Produces: `argus-logpush.service`, `argus-logpush.timer`, `/etc/argus/logpush.json` com permissão restrita.

- [ ] **Step 1: Add the dependency**

Em `install.sh`, na lista de pacotes apt (junto de `python3-bcrypt`), acrescentar:

```bash
python3-boto3
```

- [ ] **Step 2: Copy the new modules**

Junto dos demais `copy_if_exists`:

```bash
copy_if_exists "core/logpush.py"                      "$BASE_DIR/logpush.py"
copy_if_exists "core/logpush_config.py"               "$BASE_DIR/logpush_config.py"
copy_if_exists "core/logpush_fmt.py"                  "$BASE_DIR/logpush_fmt.py"
mkdir -p "$BASE_DIR/logpush_dest"
copy_if_exists "core/logpush_dest/__init__.py"        "$BASE_DIR/logpush_dest/__init__.py"
copy_if_exists "core/logpush_dest/base.py"            "$BASE_DIR/logpush_dest/base.py"
copy_if_exists "core/logpush_dest/s3.py"              "$BASE_DIR/logpush_dest/s3.py"
copy_if_exists "core/logpush_dest/webhook.py"         "$BASE_DIR/logpush_dest/webhook.py"
```

- [ ] **Step 3: Create the config with restricted permission**

Depois da seção do threatintel (mesma postura de segredo):

```bash
# Config do logpush: guarda credencial de bucket e URL de webhook. Só o app user
# lê e escreve (a página Logpush grava por aqui); "outros" não têm acesso.
if [ ! -f "$BASE_DIR/logpush.json" ]; then
  echo '{}' > "$BASE_DIR/logpush.json"
fi
chown "root:$APP_USER" "$BASE_DIR/logpush.json" 2>/dev/null || true
chmod 640 "$BASE_DIR/logpush.json"
setfacl -m "u:$APP_USER:rw" "$BASE_DIR/logpush.json" 2>/dev/null \
  && ok "Logpush: configurável pela Web" \
  || warn "setfacl indisponível — a página Logpush ficará somente leitura"
```

- [ ] **Step 4: Create the systemd timer**

Junto das demais units:

```bash
cat > /etc/systemd/system/argus-logpush.service << LPSERVICE
[Unit]
Description=Argus — envio de logs para destino externo
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=$APP_USER
Environment=ARGUS_BASE=$BASE_DIR
Environment=PYTHONPATH=$BASE_DIR
ExecStart=$PYTHON_BIN $BASE_DIR/logpush.py
# O serviço só precisa LER os logs e ESCREVER o próprio estado.
ProtectSystem=full
ProtectHome=yes
NoNewPrivileges=yes
PrivateTmp=yes
ReadWritePaths=$BASE_DIR/store
LPSERVICE

cat > /etc/systemd/system/argus-logpush.timer << LPTIMER
[Unit]
Description=Argus — logpush a cada 5 minutos

[Timer]
OnBootSec=3min
OnUnitActiveSec=5min
Unit=argus-logpush.service

[Install]
WantedBy=timers.target
LPTIMER

systemctl daemon-reload
systemctl enable --now argus-logpush.timer 2>/dev/null \
  && ok "Timer do logpush ativo (a cada 5 min)" \
  || warn "não consegui ativar o timer do logpush"
```

- [ ] **Step 5: Add the log group so the service can read**

O `argus-web` roda como `$APP_USER`, e os logs são `750 root:adm`. Para o serviço ler:

```bash
usermod -aG adm "$APP_USER" 2>/dev/null \
  && ok "$APP_USER no grupo adm (leitura dos logs)" \
  || warn "não consegui adicionar $APP_USER ao grupo adm"
```

- [ ] **Step 6: Bump the version**

```bash
echo "1.1.0" > VERSION
```

MINOR: recurso novo sem quebra de compatibilidade.

- [ ] **Step 7: Verify the installer**

Run: `bash -n install.sh && shellcheck -S warning install.sh`
Expected: sem erros

- [ ] **Step 8: Commit**

```bash
git add install.sh VERSION
git commit -m "feat(logpush): timer systemd, permissoes e dependencia (v1.1.0)"
```

---

### Task 10: Documentação

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Add the section**

Na tabela "Tudo pela web", acrescentar a linha:

```markdown
| **Logpush** | Enviar os logs para um bucket S3 ou para um chat (Google Chat, Slack, Discord, Teams, Telegram). Escolhe o que sai, e no chat quais severidades. Útil quando o Argus roda fora do seu ambiente. |
```

E, após a seção do mapa de correlação, acrescentar:

```markdown
## Levar os logs para fora

O Argus costuma rodar longe de quem o acompanha — numa VPS, de propósito, para
enxergar a empresa como um estranho enxerga. O **Logpush** resolve isso: o que ele
registra segue para onde você já olha.

Para um **bucket S3**, cada evento vira um arquivo — pronto para o SIEM consumir.
Para um **chat**, vira uma mensagem legível:

> 🔴 CRÍTICO · Nova porta exposta
> campanha: RIOCARD · ip: 104.18.9.141 · port: 443
> 15/08/2026 13:35

Os logs continuam na máquina (a rotação semanal segue valendo); o que vai para fora
é uma cópia. Se o destino cair, nada se perde: o envio recomeça de onde parou.
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: secao de logpush no README"
```

---

## Self-Review

**Cobertura do spec:**

| Requisito do spec | Task |
|---|---|
| §4 arquitetura em componentes | 1, 2, 4, 5, 6 |
| §5 sete origens selecionáveis | 1 (catálogo), 6 (leitura), 8 (interface) |
| §6 S3 write-once + anticolisão | 4 |
| §6 stdout como objeto único | 6 (`estruturado: False`) |
| §7 seis plataformas de webhook | 3 (payload), 5 (envio) |
| §7 checkboxes de severidade + aviso de flood | 1, 5, 8 |
| §8 ponteiro (inode, posição) | 6 |
| §8 rotação com `.log.1` | 6 (`_pendente_rotacionado`) |
| §8 ponteiro só avança após confirmação | 6 (`executar`) |
| §8 teto por ciclo | 6 (`MAX_POR_CICLO`) |
| §9 segredo mascarado | 1 (`mascarar`), 7 (API) |
| §9 anti-SSRF | 1 (`url_segura`), 5, 7 |
| §9 RBAC + auditoria | 7 |
| §10 interface completa | 8 |
| §11 matriz de testes | 1, 2, 3, 4, 5, 6, 7, 8 |

Sem lacunas.

**Consistência de tipos:** `Mensagem` definida na Task 2 é usada com os mesmos campos nas Tasks 3, 4, 5 e 6. `LogPushError` levantada nas Tasks 4 e 5, capturada na 6 e na 7. `severidades_ligadas`/`url_segura` definidas na Task 1, usadas na 5 e 7. `para_chat` definida na 3, usada na 5.

**Placeholders:** nenhum — todo passo que altera código traz o código.
